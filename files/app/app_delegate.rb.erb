class AppDelegate
  def applicationDidFinishLaunching(notification)
    app_name = "<%= name.gsub('-app-launcher', '') %>"
    unless NSWorkspace.sharedWorkspace.launchApplication(app_name)
      NSLog "Failed to launch #{app_name} app"
    end
  end
end
